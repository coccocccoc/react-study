import ReactDOM from 'react-dom/client';
import App from './App';

// 프로젝트의 시작점
// react app이 실행될 때 제일 먼저 찾는 파일

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
);

